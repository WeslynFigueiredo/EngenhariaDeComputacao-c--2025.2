#include <iostream>
using namespace std;

int main() {
  int numero;
 
    cout << "digite um numero inteiro:" << endl;
    cin >> numero;
    
    numero = abs(numero);
    int soma = 0;
    
    while (numero > 0) {
        soma += numero % 10;
        numero /= 10;
    }
    
    cout << "A soma dos digitos é: " << soma << endl;
    
    return 0;
}
